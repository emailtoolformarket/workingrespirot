# --- Email Marketing SaaS API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM / database
sqlalchemy==2.0.36

# Validation & settings
pydantic==2.10.4
python-dotenv==1.0.1

# Security: JWT + password hashing
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt>=4.1 (reads bcrypt.__about__),
# so we pin bcrypt to the last fully compatible release.
bcrypt==4.0.1

# Form parsing (required by FastAPI for OAuth2 password flows)
python-multipart==0.0.20
